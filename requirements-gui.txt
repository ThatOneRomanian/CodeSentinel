# GUI Development Requirements

Add the following to your Python environment to run the FastAPI backend:

```bash
pip install fastapi uvicorn python-multipart
```

Or add to `requirements-gui.txt`:

```
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0
